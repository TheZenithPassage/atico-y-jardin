# Data Model

Not applicable. This feature introduces no domain entities, persistence model,
API payloads, schema changes, browser storage, external contracts, or structured
feature data.

The existing public landing presentation data and translations may receive
small hero-copy fields for rendering the static public hero, but they do not
change the product domain model or any persisted data contract.
